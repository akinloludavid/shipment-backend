export enum UserGender {
    MALE = 'MALE',
    FEMALE = 'FEMALE'
}

export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum Environment {
    Local = 'local',
    Development = 'development',
    Production = 'production'
}

